# FE-03 walkthrough — jobs UI foundation

## Tasks

Закрыты `FE-03-T001`–`T008`.

## Сделано

- Добавлен `contracts/job-api.md`: snapshots, lifecycle states, SSE и recovery.
- Добавлен защищённый `/cabinet/jobs` с созданием task, status/stages, cancel UX
  и navigation entry.
- Next route handlers передают access token из httpOnly cookie в будущие backend
  endpoints, включая SSE proxy.
- SSE reconnect использует экспоненциальную задержку и fallback polling 5 sec;
  последний активный job ID хранится в `sessionStorage` для reload recovery.

## Проверки

- `ui/node_modules/.bin/tsc.cmd --noEmit` — passed.
- `node --test ui/tests/jobs-api.mjs` — passed against local FastAPI + Next.js:
  registration, cookie session, job creation and cancellation.
- `git diff --check` — passed.

## Ограничения и следующий шаг

Backend adapter реализован в продолжении BE-07. Дальнейшие UI scenarios для
history/artifacts относятся к FE-04 и не расширяют этот контракт.
