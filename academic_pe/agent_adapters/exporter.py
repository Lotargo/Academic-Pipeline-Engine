from __future__ import annotations


def contract_guidance() -> str:
    return (
        "Exporter: format the artifact according to the contract; do not add title pages or citation sections unless "
        "required. Preserve artifact-native headings, spacing, and output constraints."
    )
